# Changesets

This folder is managed by [Changesets](https://github.com/changesets/changesets).

Run `npm run changeset` to record a change. On release, `npm run version`
consumes the pending changesets to bump the version and update `CHANGELOG.md`,
and `npm run release` builds and publishes to npm.
